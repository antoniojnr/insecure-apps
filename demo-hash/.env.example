# Arquivo de variáveis de ambiente
# Copie este arquivo para .env.local e configure os valores

PORT=3000
NODE_ENV=development

# JWT Secret - use uma string forte e segura em produção
JWT_SECRET=sua_chave_secreta

# Duração do token JWT (em horas)
JWT_EXPIRES_IN=24h

# Configurações de banco de dados (exemplo com SQLite para desenvolvimento)
DB_TYPE=sqlite
DB_PATH=./data/users.db

# Configurações de CORS
CORS_ORIGIN=http://localhost:3000
